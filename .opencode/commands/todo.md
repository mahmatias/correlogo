---
description: Add items to the persistent TODO list
---

Add the following item(s) to TODO.md in the repository root:

$ARGUMENTS

Place them at the top of the file under a new section with today's date (like "## YYYY-MM-DD — <summary>"), above the existing content. Format each item as:
- [ ] descrição da tarefa

If the user provides multiple items, add each one as a separate bullet point. Read the current TODO.md first, then edit it to insert the new section at the top.
